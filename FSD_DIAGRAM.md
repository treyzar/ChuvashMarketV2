# 🏗️ FSD Architecture Diagram

## Слои приложения

```
┌─────────────────────────────────────────────────────────────────┐
│                    APP Layer                                     │
│         (Инициализация приложения, провайдеры)                  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                   PAGES Layer                                    │
│              (Полные страницы приложения)                        │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                   WIDGETS Layer                                  │
│        (Переиспользуемые блоки, Header, Footer, etc)             │
└─────────────────────────────────────────────────────────────────┘
                              ↓
        ┌─────────────────────┴──────────────────────┐
        ↓                                            ↓
┌─────────────────────┐                  ┌─────────────────────┐
│  FEATURES Layer     │ ←─────────────→  │  ENTITIES Layer     │
│  (Фичи приложения)  │                  │ (Бизнес-сущности)   │
└─────────────────────┘                  └─────────────────────┘
        ↓                                            ↓
        └─────────────────────┬──────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              SHARED Layer                                        │
│   (Переиспользуемый код, компоненты, утилиты)                  │
└─────────────────────────────────────────────────────────────────┘
```

## Матрица зависимостей

|              | App | Pages | Widgets | Features | Entities | Shared |
| ------------ | --- | ----- | ------- | -------- | -------- | ------ |
| **App**      | ✅  | ❌    | ❌      | ❌       | ❌       | ✅     |
| **Pages**    | ❌  | ❌    | ✅      | ✅       | ✅       | ✅     |
| **Widgets**  | ❌  | ❌    | ❌      | ✅       | ✅       | ✅     |
| **Features** | ❌  | ❌    | ❌      | ❌       | ✅       | ✅     |
| **Entities** | ❌  | ❌    | ❌      | ❌       | ❌       | ✅     |
| **Shared**   | ❌  | ❌    | ❌      | ❌       | ❌       | ✅     |

✅ = Можно импортировать  
❌ = Нельзя импортировать

## Пример импортов

### ❌ Запрещено

```js
// ❌ Features не может импортировать из Widgets
import { Header } from "@widgets";
export const SearchFeature = () => {
  return <Header />; // ОШИБКА!
};

// ❌ Entities не может импортировать из Features
import { AddToCart } from "@features";
export const Product = () => {
  return <AddToCart />; // ОШИБКА!
};

// ❌ Pages не может импортировать из других Pages
import { HomePage } from "@pages/HomePage";
export const ProductPage = () => {
  return <HomePage />; // ОШИБКА!
};
```

### ✅ Разрешено

```js
// ✅ Pages может импортировать из Widgets, Features, Entities, Shared
import { Header } from "@widgets";
import { ProductList } from "@features";
import { Product } from "@entities";
import { Button } from "@shared/ui";

export const HomePage = () => {
  return (
    <div>
      <Header />
      <ProductList />
    </div>
  );
};

// ✅ Widgets может импортировать из Features, Entities, Shared
import { AddToCart } from "@features";
import { Product } from "@entities";
import { formatPrice } from "@shared/lib";

export const ProductCard = ({ product }) => {
  return (
    <div>
      <p>{product.name}</p>
      <p>{formatPrice(product.price)}</p>
      <AddToCart productId={product.id} />
    </div>
  );
};

// ✅ Features может импортировать из Entities и Shared
import { Product } from "@entities";
import { Button } from "@shared/ui";

export const AddToCartButton = ({ productId }) => {
  // Feature логика
  return <Button>Добавить в корзину</Button>;
};

// ✅ Entities может импортировать только из Shared
import { Button } from "@shared/ui";
import { formatPrice } from "@shared/lib";

export const ProductCard = ({ product }) => {
  return (
    <div>
      <p>{formatPrice(product.price)}</p>
      <Button>Подробнее</Button>
    </div>
  );
};

// ✅ Shared может импортировать только из Shared
import { formatPrice } from "@shared/lib";

export const Button = ({ price, ...props }) => {
  return <button>Купить за {formatPrice(price)}</button>;
};
```

## Структура каждого слоя

```
Module/
├── ui/              # UI компоненты
│   ├── Component1.jsx
│   ├── Component2.jsx
│   ├── Component1.module.css
│   └── Component2.module.css
├── model/           # Состояние, хуки, типы
│   ├── store.js
│   ├── useModule.js
│   └── types.ts
├── api/             # API запросы (опционально)
│   └── moduleAPI.js
├── lib/             # Утилиты специфичные для модуля (опционально)
│   └── helpers.js
├── types/           # Типы (опционально)
│   └── types.ts
├── index.js         # Barrel export (публичный API)
└── README.md        # Документация модуля
```

## Пример структуры для Feature

```
features/
├── AddToCart/
│   ├── ui/
│   │   ├── AddToCartButton.jsx
│   │   ├── AddToCartModal.jsx
│   │   └── AddToCart.module.css
│   ├── model/
│   │   ├── useAddToCart.js
│   │   ├── cartSlice.js
│   │   └── types.ts
│   ├── api/
│   │   └── cartAPI.js
│   ├── lib/
│   │   └── helpers.js
│   ├── index.js
│   └── README.md
│
├── Search/
│   ├── ui/
│   ├── model/
│   └── index.js
│
└── index.js (главный экспорт)
```

## Пример barrel export

```js
// features/AddToCart/index.js
export { AddToCartButton } from './ui/AddToCartButton'
export { AddToCartModal } from './ui/AddToCartModal'
export { useAddToCart } from './model/useAddToCart'
export type { AddToCartProps } from './model/types'

// Использование:
import { AddToCartButton, useAddToCart } from '@features/AddToCart'
```

## Циклические зависимости

⚠️ **Избегайте циклических зависимостей!**

```js
// ❌ Плохо: создает циклическую зависимость
// features/Feature1.js
import { Feature2 } from "@features/Feature2";

// features/Feature2.js
import { Feature1 } from "@features/Feature1";

// ✅ Хорошо: используйте Entities в качестве промежуточного слоя
// features/Feature1.js
import { Product } from "@entities";

// features/Feature2.js
import { Product } from "@entities";
```

## Линтер для FSD

Рекомендуется использовать `eslint-plugin-fsd-architecture` для проверки правил.

```bash
npm install --save-dev eslint-plugin-fsd-architecture
```

Конфигурация в `.eslintrc`:

```json
{
  "plugins": ["fsd-architecture"],
  "rules": {
    "fsd-architecture/no-cross-layers": "error"
  }
}
```
