# 🎨 Widgets Layer

Слой **Widgets** содержит независимые, переиспользуемые блоки интерфейса, которые комбинируют несколько фич и сущностей.

## Назначение

- Композиция features и entities в отдельные блоки
- Предоставление готовых к использованию компонентов для pages
- Управление локальным состоянием виджета
- Переиспользование на разных страницах

## Структура

```
widgets/
├── ProductCard/
│   ├── ui/
│   │   ├── ProductCard.jsx
│   │   └── ProductCard.module.css
│   ├── hooks/
│   │   └── useProductCard.js
│   ├── index.js
│   └── README.md
├── Header/
│   ├── ui/
│   ├── model/
│   └── index.js
└── README.md
```

## Правила

✅ **Разрешено:**

- Использование features
- Использование entities
- Использование shared
- Управление локальным состоянием

❌ **Запрещено:**

- Импорты из pages
- Бизнес-логика (она в features)
- Использование других widgets (избегать цирkulярных зависимостей)

## Структура внутри виджета

```
ProductCard/
├── ui/              # UI компоненты
├── model/           # Состояние, хуки, типы
├── types/           # TypeScript интерфейсы
├── index.js         # Barrel export
└── README.md        # Документация компонента
```

## Примеры использования

```jsx
// Header/Header.jsx
import { SearchFeature } from "@features";
import { CartIcon } from "@entities";

export const Header = () => (
  <header>
    <SearchFeature />
    <CartIcon />
  </header>
);
```

## Экспорт

```js
// index.js
export { ProductCard } from "./ui/ProductCard";
export { useProductCard } from "./hooks/useProductCard";
```
