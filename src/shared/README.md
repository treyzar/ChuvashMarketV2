# 🎁 Shared Layer

Слой **Shared** содержит переиспользуемый код, который не связан с бизнес-логикой приложения.

## Назначение

- Переиспользуемые UI компоненты
- Утилиты и помощники
- Типы TypeScript
- Константы приложения
- Конфигурация и провайдеры
- API клиенты

## Структура

```
shared/
├── ui/              # Переиспользуемые UI компоненты
│   ├── Button/
│   ├── Modal/
│   ├── Card/
│   └── index.js
├── lib/             # Утилиты и помощники
│   ├── api.js
│   ├── formatters.js
│   ├── validators.js
│   └── index.js
├── types/           # Глобальные типы
│   ├── common.ts
│   └── index.ts
├── constants/       # Константы
│   ├── routes.js
│   ├── config.js
│   └── index.js
├── api/             # API клиенты
│   ├── axios.js
│   └── index.js
└── README.md
```

## Правила

✅ **Разрешено:**

- Общие компоненты UI
- Утилиты
- Константы
- Типы

❌ **Запрещено:**

- Импорты из других слоев
- Специфичная для фичи логика
- Зависимость от бизнес-логики

## Основные поддиректории

### 📦 ui/

Переиспользуемые UI компоненты:

- Button
- Input
- Select
- Modal
- Card
- Layout
- и т.д.

Каждый компонент в отдельной папке:

```
Button/
├── Button.jsx
├── Button.module.css
└── index.js
```

### 🛠️ lib/

Утилиты и помощники:

- `api.js` - конфигурация HTTP клиента
- `formatters.js` - форматирование данных
- `validators.js` - валидация
- `hooks.js` - переиспользуемые хуки
- `helpers.js` - вспомогательные функции

### 📝 types/

Глобальные типы TypeScript:

- Общие интерфейсы
- Типы API ответов
- Типы конфигурации

### 📌 constants/

Константы приложения:

- Маршруты
- Конфигурация
- API endpoints
- Допустимые значения

### 🌐 api/

API клиенты и конфигурация:

- Инстанс axios
- Интерцепторы
- API сервисы

## Примеры использования

```jsx
// Импорт компонента
import { Button } from '@shared/ui'

// Импорт утилиты
import { formatPrice } from '@shared/lib'

// Импорт константы
import { ROUTES } from '@shared/constants'

// Импорт типа
import type { ApiResponse } from '@shared/types'
```

## Экспорт

```js
// shared/ui/index.js
export { Button } from './Button'
export { Modal } from './Modal'
export { Card } from './Card'

// shared/lib/index.js
export { formatPrice } from './formatters'
export { validateEmail } from './validators'
export { useLocalStorage } from './hooks'

// shared/constants/index.js
export { ROUTES } from './routes'
export { API_CONFIG } from './config'

// shared/types/index.js
export type { ApiResponse } from './common'
```
