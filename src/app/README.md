# 🏛️ App Layer

Слой **App** — это корневой слой приложения, отвечающий за инициализацию и глобальную конфигурацию.

## Назначение

- Инициализация приложения
- Подключение глобальных провайдеров (Redux, React Query, Theme Provider и т.д.)
- Глобальная конфигурация приложения
- Корневая обработка ошибок
- Настройка маршрутизации

## Структура

```
app/
├── providers/      # Глобальные провайдеры (Redux, Theme, Router и т.д.)
├── config/         # Конфигурация приложения
├── App.jsx         # Корневой компонент приложения
└── README.md       # Документация слоя
```

## Правила

✅ **Разрешено:**

- Подключение глобальных провайдеров
- Конфигурация окружения
- Инициализация сервисов
- Обработка ошибок на уровне приложения

❌ **Запрещено:**

- Бизнес-логика
- Компоненты UI (кроме корневого App)
- Импорты из других слоев (кроме shared, widgets, features)

## Примеры использования

```jsx
// App.jsx
import { ThemeProvider } from "@shared/providers";
import { AppRoutes } from "@pages";

export const App = () => (
  <ThemeProvider>
    <AppRoutes />
  </ThemeProvider>
);
```

## Стандартные файлы

- `providers/index.js` — экспорт всех провайдеров
- `config/index.js` — экспорт конфигурации
